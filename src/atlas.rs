



#[cfg(test)]
mod tests {
	use super::*;
    #[test]
    fn test_atlas() {
        let atlas_size = (16, 16);
		let inset = (0.01, 0.01);
		let texture_ids = [164, 165, 166, 167, 168, 169]; // Specify texture IDs for each face

		
    }
}
// Example usage
